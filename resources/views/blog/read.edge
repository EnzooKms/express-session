@layout('layouts/main')
@set('title', 'main')
@set('css', ['https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/css/bootstrap.min.css',
'../../resources/css/blog/read.css'])
@set('js', [])

@section('body')

<div class="container d-flex justify-content-center align-items-center">
  <article>
    <h1>{{data.title}}</h1>
  
    <img src="../../uploads/{{data.file}}" alt="">
    <p>{{data.description}}</p>
  
    <small>Créer par : {{data.author}} </small>
  
  </article>
</div>

@endsection